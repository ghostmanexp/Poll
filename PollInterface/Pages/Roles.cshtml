@page
@model PollInterface.Pages.RolesModel
@*@model Poll.Models.Role*@
@{
}
<div class="text-center">
    <h1 class="display-4">Roles</h1>
    
</div>


<div class="form-control">

</div>
<div class="text-body">
    @if (Model.Roles.Count > 0)
    {
        @foreach(var i in Model.Roles)
        {
            @Html.DisplayFor(m=> i.Name);
        }
    }
</div>